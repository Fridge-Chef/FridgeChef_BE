jasypt:
  encryptor:
    password: ${JASYPT_PASSWORD}
    bean: jasyptStringEncryptor

spring:
  profiles:
    default: local
  output:
    ansi:
      enabled: always

logging:
  level:
    web: debug

springdoc:
  default-support-form-data: false