spring:
  config:
    activate:
      on-profile: dev
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        order_inserts: true
        order_updates: true
        connection:
          pool_size: 70
        jdbc:
          batch_size: 1000
        show_sql:
        format_sql:
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:oracle:thin:@fridgechef_high?TNS_ADMIN=(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.ap-chuncheon-1.oraclecloud.com))(connect_data=(service_name=g9df66667042fa2_fridgechef_high.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
    username: ENC(VhOrGvMaPi2etM/K8z6Kbw==)
    password: ENC(mE7S46BWUTZS1ES17svBz72u6sA6c0No)
    driver-class-name: oracle.jdbc.OracleDriver

logging:
  level:
    org.springframework.core.env.PropertySourcesPropertyResolver: DEBUG
    org:
      springframework:
        core:
          env:
            PropertySourcesPropertyResolver: DEBUG